# 숏츠 디톡스 (Shorts Detox) — 디자인 설계

## 앱 개요
숏츠 중독 패턴을 감지하고, 사용자가 스스로 인식하여 뇌 인지 개선 활동 또는 명상으로 전환하도록 유도하는 디지털 웰빙 앱.

---

## 브랜드 & 색상

| 토큰 | Light | Dark | 용도 |
|------|-------|------|------|
| `primary` | `#6C63FF` | `#8B85FF` | 주요 액션, 강조 |
| `background` | `#F8F7FF` | `#0F0E1A` | 화면 배경 |
| `surface` | `#FFFFFF` | `#1A1929` | 카드, 모달 배경 |
| `foreground` | `#1A1A2E` | `#EEEEFF` | 주요 텍스트 |
| `muted` | `#7B7B9A` | `#9B9BBF` | 보조 텍스트 |
| `border` | `#E8E6FF` | `#2E2C4A` | 구분선 |
| `success` | `#4CAF82` | `#5DD99A` | 달성, 완료 |
| `warning` | `#FF9F43` | `#FFB86C` | 경고, 주의 |
| `error` | `#FF6B6B` | `#FF8585` | 오류, 위험 |
| `accent` | `#FF6B9D` | `#FF85B3` | 뇌 활동 강조 |

**컨셉**: 보라색 계열의 차분하고 신뢰감 있는 디지털 웰빙 팔레트. 숏츠의 자극적인 빨간/주황과 대비되는 차분한 보라-인디고 계열.

---

## 화면 목록

### 1. 온보딩 / 로그인 화면 (`/login`)
- 앱 로고 + 슬로건
- 구글 소셜 로그인 버튼
- 간단한 앱 소개 (3단계 온보딩 슬라이드)

### 2. 홈 / 대시보드 (`/(tabs)/index`)
- 오늘의 숏츠 시청 시간 원형 게이지
- 목표 대비 현재 사용량 진행바
- 최근 디톡스 활동 요약 카드
- "지금 디톡스 시작" 버튼
- 연속 달성 스트릭 배지

### 3. 타이머 / 추적 화면 (`/(tabs)/tracker`)
- 숏츠 시청 타이머 (수동 시작/정지)
- 플랫폼 선택 (YouTube Shorts, TikTok, Instagram Reels, 기타)
- 세션 기록 리스트
- 알림 설정 (X분마다 알림)

### 4. 디톡스 인터럽트 화면 (`/detox`)
- 전체 화면 모달 (숏츠 시청 중단 유도)
- 3가지 선택지 카드:
  - 🧠 뇌 인지 개선 활동하기
  - 🧘 명상하기
  - ✕ 그냥 끄기
- 현재 시청 시간 표시
- 동기부여 메시지

### 5. 뇌 인지 개선 게임 선택 (`/brain`)
- 게임 카테고리 카드 그리드:
  - 기억력 게임 (카드 뒤집기)
  - 집중력 게임 (색상 매칭)
  - 반응속도 게임 (타겟 탭)
  - 숫자 순서 게임 (N-Back)
- 난이도 선택
- 최고 점수 표시

### 6. 기억력 게임 (`/brain/memory`)
- 카드 뒤집기 매칭 게임
- 6x4 그리드 (난이도별 조정)
- 타이머 + 시도 횟수
- 완료 시 점수 및 결과

### 7. 집중력 게임 (`/brain/focus`)
- 색상/단어 스트룹 테스트
- 연속 정답 카운터
- 제한 시간 내 최대 정답

### 8. 반응속도 게임 (`/brain/reaction`)
- 화면에 나타나는 타겟 탭하기
- 반응 시간 ms 측정
- 평균 반응 시간 통계

### 9. 명상 가이드 (`/meditation`)
- 호흡 애니메이션 (들숨/날숨 원형 펄스)
- 명상 유형 선택 (4-7-8 호흡, 박스 호흡, 자유 호흡)
- 타이머 (1분, 3분, 5분, 10분)
- 배경 사운드 (자연음, 화이트노이즈)
- 완료 시 격려 메시지

### 10. 통계 화면 (`/(tabs)/stats`)
- 주간/월간 숏츠 시청 시간 바 차트
- 디톡스 활동 완료 횟수
- 절약된 시간 표시
- 스트릭 캘린더

### 11. 설정 화면 (`/(tabs)/settings`)
- 프로필 (구글 계정 정보)
- 일일 목표 시간 설정
- 알림 설정 (주기, 시간대)
- 테마 설정 (라이트/다크)
- 로그아웃

---

## 핵심 사용자 플로우

### 플로우 1: 첫 사용
앱 실행 → 온보딩 슬라이드 → 구글 로그인 → 목표 설정 → 홈 대시보드

### 플로우 2: 숏츠 시청 추적
홈 → 타이머 탭 → 플랫폼 선택 → 타이머 시작 → X분 후 알림 → 디톡스 인터럽트 화면

### 플로우 3: 뇌 인지 개선
디톡스 인터럽트 → "뇌 인지 개선" 선택 → 게임 선택 → 게임 플레이 → 결과 → 홈으로

### 플로우 4: 명상
디톡스 인터럽트 → "명상하기" 선택 → 명상 유형/시간 선택 → 명상 진행 → 완료 → 홈으로

---

## 탭 바 구성

| 탭 | 아이콘 | 화면 |
|----|--------|------|
| 홈 | house.fill | 대시보드 |
| 추적 | timer | 타이머/추적 |
| 뇌 활동 | brain.head.profile | 게임 선택 |
| 통계 | chart.bar.fill | 통계 |
| 설정 | gearshape.fill | 설정 |

---

## 레이아웃 원칙

- **모바일 세로 (9:16)** 기준, 한 손 조작 최적화
- 주요 액션 버튼은 화면 하단 엄지 영역에 배치
- 카드 기반 UI, 둥근 모서리 (radius: 16-24px)
- 충분한 여백으로 시각적 호흡감 확보
- 다크모드 완전 지원
